import { GetStylesComponent } from './../../styles';

export const StylesTodoListComponent = {
    TodoListPadding: {
        padding: '2rem 0'
    }
};

export default GetStylesComponent(StylesTodoListComponent);
